import React from "react"; import { motion } from "framer-motion";

export default function RedStringLovePage() { return ( <div className="min-h-screen bg-gradient-to-b from-black via-purple-950 to-black text-white flex flex-col items-center justify-center p-6 overflow-hidden">

{/* Estrellas de fondo */}
  <div className="absolute inset-0 overflow-hidden">
    {[...Array(40)].map((_, i) => (
      <motion.div
        key={i}
        className="absolute w-1 h-1 bg-white rounded-full"
        initial={{
          opacity: 0,
          y: Math.random() * 800,
          x: Math.random() * 1200,
        }}
        animate={{
          opacity: [0, 1, 0],
          y: "+=60",
        }}
        transition={{
          duration: 4 + Math.random() * 4,
          repeat: Infinity,
        }}
      />
    ))}
  </div>

  {/* Luna animada */}
  <motion.div
    className="text-7xl mb-8"
    animate={{ scale: [1, 1.1, 1] }}
    transition={{ duration: 4, repeat: Infinity }}
  >
    🌙
  </motion.div>

  {/* Hilo rojo con nombres */}
  <div className="flex items-center justify-center gap-6 mb-12 relative">
    
    <motion.div
      initial={{ opacity: 0, x: -80 }}
      animate={{ opacity: 1, x: 0 }}
      transition={{ duration: 2 }}
      className="bg-white/10 backdrop-blur-lg rounded-2xl p-6 shadow-xl"
    >
      <h2 className="text-2xl font-bold text-pink-300">Aby</h2>
    </motion.div>

    <motion.div
      className="h-1 w-40 bg-red-500"
      animate={{ scaleX: [1, 1.3, 1] }}
      transition={{ duration: 2, repeat: Infinity }}
    />

    <motion.div
      initial={{ opacity: 0, x: 80 }}
      animate={{ opacity: 1, x: 0 }}
      transition={{ duration: 2 }}
      className="bg-white/10 backdrop-blur-lg rounded-2xl p-6 shadow-xl"
    >
      <h2 className="text-2xl font-bold text-blue-300">David</h2>
    </motion.div>
  </div>

  {/* Mensaje romántico */}
  <motion.div
    initial={{ opacity: 0, y: 50 }}
    animate={{ opacity: 1, y: 0 }}
    transition={{ duration: 2 }}
    className="max-w-2xl text-center bg-white/5 p-8 rounded-2xl backdrop-blur-lg shadow-2xl"
  >
    <p className="text-lg leading-relaxed text-purple-100">
      Dicen que existe un hilo rojo invisible que conecta a las personas que están destinadas a encontrarse.
      No importa el tiempo, la distancia o los caminos que recorran.
      
      <br /><br />

      Tal vez el universo escribió nuestras historias en momentos distintos,
      pero en algún punto decidió cruzarlas.

      <br /><br />

      Y desde entonces, cada sonrisa, cada conversación y cada momento compartido
      hace que ese hilo se vuelva más fuerte.

      <br /><br />

      Porque hay conexiones que no se explican…
      solo se sienten.
    </p>
  </motion.div>

  {/* Corazones flotando */}
  <div className="absolute inset-0 pointer-events-none">
    {[...Array(15)].map((_, i) => (
      <motion.div
        key={i}
        className="absolute text-pink-400 text-xl"
        initial={{
          y: 900,
          x: Math.random() * 1200,
          opacity: 0,
        }}
        animate={{
          y: -100,
          opacity: [0, 1, 0],
        }}
        transition={{
          duration: 6 + Math.random() * 4,
          repeat: Infinity,
          delay: Math.random() * 5,
        }}
      >
        ❤️
      </motion.div>
    ))}
  </div>

</div>

); }
